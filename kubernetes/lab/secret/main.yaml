apiVersion: v1
kind: Secret
metadata:
  name: weather-api-secret
  namespace: lab-devops
type: Opaque
stringData:
  # stringData aceita texto plano aqui, mas o Kubernetes guarda em base64
  # no etcd (não é criptografia — veja 06-configmap-e-secret.md).
  OPENWEATHER_API_KEY: "SUA_API_KEY_AQUI"
